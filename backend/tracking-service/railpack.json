{
  "steps": {
    "build": {
      "cmds": [
        "yarn workspace @flexbox/types build && yarn workspace @flexbox/constants build && yarn workspace @flexbox/tracking-service build"
      ]
    }
  },
  "deploy": {
    "cmd": "node backend/tracking-service/dist/index.js"
  }
}