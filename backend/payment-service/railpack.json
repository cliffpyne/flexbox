{
  "steps": {
    "build": {
      "cmds": [
        "yarn workspace @flexbox/types build && yarn workspace @flexbox/constants build && yarn workspace @flexbox/payment-service build"
      ]
    }
  },
  "deploy": {
    "cmd": "node backend/payment-service/dist/index.js"
  }
}