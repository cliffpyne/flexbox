{
  "steps": {
    "build": {
      "cmds": [
        "yarn workspace @flexbox/types build && yarn workspace @flexbox/constants build && yarn workspace @flexbox/notification-service build"
      ]
    }
  },
  "deploy": {
    "cmd": "node backend/notification-service/dist/index.js"
  }
}